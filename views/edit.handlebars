<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit {{photo.title}} - Digital Media Catalog</title>
</head>
<body>
    <div style="text-align: right; padding: 10px;">
        Welcome, {{user.name}}! | <a href="/logout">Logout</a>
    </div>
    
    <h1>Edit Photo</h1>
    
    <form method="POST" action="/photo/{{photo.id}}/update">
        <p>
            <label for="title">Title:</label><br>
            <input type="text" id="title" name="title" value="{{photo.title}}" size="50">
        </p>
        
        <p>
            <label for="description">Description:</label><br>
            <textarea id="description" name="description" rows="4" cols="50">{{photo.description}}</textarea>
        </p>
        
        <p>
            <label for="visibility">Visibility:</label><br>
            <select id="visibility" name="visibility">
                <option value="public" {{#if (eq photo.visibility "public")}}selected{{/if}}>Public</option>
                <option value="private" {{#if (eq photo.visibility "private")}}selected{{/if}}>Private</option>
            </select>
        </p>
        
        <p>
            <button type="submit">Submit</button>
        </p>
    </form>
    
    <p><a href="/photo/{{photo.id}}">Cancel</a></p>
</body>
</html>
