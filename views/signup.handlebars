<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Sign Up - Digital Media Catalog</title>
</head>
<body>
    <h1>Sign Up</h1>
    
    {{#if error}}
    <p style="color: red;">{{error}}</p>
    {{/if}}
    
    <form method="POST" action="/signup">
        <p>
            <label for="name">Name:</label><br>
            <input type="text" id="name" name="name" value="{{name}}" required size="50">
        </p>
        
        <p>
            <label for="email">Email:</label><br>
            <input type="email" id="email" name="email" value="{{email}}" required size="50">
        </p>
        
        <p>
            <label for="password">Password:</label><br>
            <input type="password" id="password" name="password" required size="50">
        </p>
        
        <p>
            <button type="submit">Sign Up</button>
        </p>
    </form>
    
    <p>Already have an account? <a href="/login">Log in here</a></p>
</body>
</html>